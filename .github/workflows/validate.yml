name: Validate

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v2

      - uses: PeterJCLaw/srcomp-validate-action@65ffcac6f621d29ff3f24cbd1a4d715e5cbf3e5d

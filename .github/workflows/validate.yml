name: Validate

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v2

      - uses: PeterJCLaw/srcomp-validate-action@cbc4070d800287ded65f6633b4f31463ce8bdaec

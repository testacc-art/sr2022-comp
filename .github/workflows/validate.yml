name: Validate

on:
  push:
#     branches:
#       - master
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v2
      - run: echo "$(pwd)"
      - uses: testacc-art/srcomp-validate-action@test-for-current-dir

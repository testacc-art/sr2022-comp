name: Validate

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v2

      - uses: PeterJCLaw/srcomp-validate-action@b07ddc100ef584150e10e0b8693bbcb97773b134

      - run: ls -la
        if: ${{ always() }}

      - run: ls -laR /home/runner/work/_actions/PeterJCLaw
        if: ${{ always() }}
